name = "sorokina-publish"
main = "publish.js"
compatibility_date = "2024-09-23"

[vars]
# Non-secret config (safe to commit)
# Secrets are set via: wrangler secret put <KEY>
# Required secrets:
#   META_TOKEN, META_ACCOUNT_ID
#   VK_TOKEN, VK_OWNER_ID
#   TG_BOT_TOKEN, TG_CHANNEL_ID
#   ALLOWED_ORIGIN
